#conainer wehre we store value
coordinates_value=(1,2,3)

print(coordinates_value[1])


#in list we can add delete data but in tuple we cannot change.
#we use tupple where data shouldnot be changed
